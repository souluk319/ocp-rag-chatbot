# Status Snapshot 2026-04-02

## Scope

This snapshot records the current local state of the `rebuild/ko-rag-core` branch.
It is intended as a handoff/reference note before further retrieval, answer-quality,
and UI cleanup work.

## Canonical Paths

- Workspace: `C:\Users\soulu\cywell\ocp-rag-chatbot`
- Canonical artifacts: `C:\Users\soulu\cywell\ocp-rag-chatbot-data`
- Mixed old artifacts backup:
  `C:\Users\soulu\cywell\ocp-rag-chatbot-data-backups\20260402-210659-mixed-artifacts-pre-full-rebuild`

## Fixed Pipeline Decisions

- Embeddings: remote endpoint via `EMBEDDING_BASE_URL`
- Local chunking/tokenizer model: `dragonkue/bge-m3-ko`
- `sentence-transformers`: `5.3.0`
- `transformers`: `4.57.6`
- `tokenizers`: `0.22.2`
- Current chunk defaults:
  - `chunk_size=160`
  - `chunk_overlap=32`

## Part 1 Status

Fresh full rebuild completed on canonical artifacts.

- `manifest_count=113`
- `collected_count=113`
- `normalized_count=23557`
- `chunk_count=95679`
- `embedded_count=95679`
- `qdrant_upserted_count=95679`
- `errors=[]`

Data quality audit result:

- `manifest_mojibake_count=0`
- `preprocessing_log_mojibake_count=0`
- `normalized_title_mojibake_count=0`
- `chunk_title_mojibake_count=0`
- `raw_html_title_mojibake_count=0`
- `bad_viewer_path_count=0`
- `bad_source_url_count=0`
- `suspicious_chunk_count=0`

Related artifact reports:

- `C:\Users\soulu\cywell\ocp-rag-chatbot-data\part1\preprocessing_log.json`
- `C:\Users\soulu\cywell\ocp-rag-chatbot-data\part1\data_quality_report.json`

## Part 2 Status

Offline retrieval sanity set is now the main gate before more UI testing.

Sanity report:

- file: `C:\Users\soulu\cywell\ocp-rag-chatbot-data\part2\sanity_report.json`
- `case_count=25`
- `expected_hit_at_1=1.0`
- `expected_hit_at_3=1.0`
- `expected_hit_at_5=1.0`
- `forbidden_free_at_1=1.0`
- `forbidden_free_at_3=1.0`
- `forbidden_free_at_5=1.0`
- `warning_free_rate=0.92`

Why `warning_free_rate` is not `1.0`:

- unsupported/external-product queries are now intentionally short-circuited
- current warnings:
  - `query appears outside OCP corpus: eks`
  - `query appears outside OCP corpus: harbor`

## Part 3 / Part 4 Current Reality

The main remaining user-visible problems are not full-corpus size problems.
They are policy/UX problems around unsupported or non-Korean pages.

Known issue 1:

- `backup_and_restore` exists and is retrieved correctly
- but the stored page is English-first, not Korean-first
- local file:
  `C:\Users\soulu\cywell\ocp-rag-chatbot-data\part1\raw_html\backup_and_restore.html`
- consequence:
  retrieval can succeed while answer quality still feels wrong in Korean UX

Known issue 2:

- citation links should open internal `/docs/ocp/4.20/ko/...`
- current code path already prefers `viewer_path`
- but this still needs live UI verification after server restart because the user
  observed web-page style behavior and a 404 flow during manual testing

Known issue 3:

- answer quality is still below acceptable user-facing quality
- retrieval sanity is no longer the main blocker for the previously failing
  intro/doc-locator/unsupported cases
- the next blocker is how to handle:
  - English-only or Korean-missing pages
  - unsupported product asks
  - internal citation/view rendering consistency

## Current Test State

Most recent full test run:

- command: `python -m unittest discover -s tests -v`
- result: `43 tests OK`

## Next Recommended Work

1. Build a report of books/pages that are not actually Korean-first even though they sit under the Korean corpus path.
2. Add answer policy for:
   - document exists but Korean body is missing
   - unsupported/external product query
3. Re-verify that every citation in the QA UI opens internal `/docs/...` content, not an external web page fallback.
4. Then resume manual QA in the UI.
