# Stage 11 Readiness Pointer

The authoritative Stage 11 readiness document lives at:

- `docs/v2/stage11-readiness.md`

This file exists so deployment-oriented preflight checks can point to a stable path inside `deployment/`.
