# Deployment Scope

This directory will document how v2 is packaged and deployed for the target environment.

Planned topics:

- external collection environment
- approved transfer bundle format
- closed-network import procedure
- reindex flow and rollback points
- runtime topology for the final assistant
