# Ingestion Scope

This directory will hold the v2 onboarding pipeline for documents.

Target responsibilities:

- mirror or sync source repositories
- normalize source files into indexable text
- generate metadata manifests
- package approved deltas for closed-network transfer

The first implementation should focus on `openshift-docs` before broader source expansion.
