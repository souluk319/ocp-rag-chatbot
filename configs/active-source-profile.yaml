version: 1
active_profile_id: ocp-validation-main-core
target_minor:
notes:
  - Validation mode currently serves the widened main-based core validation corpus.
  - This active profile is aligned with the live runtime index `s15c-core`.
  - Once a target minor is approved, switch to an operator_release profile instead of rewriting the pipeline.
