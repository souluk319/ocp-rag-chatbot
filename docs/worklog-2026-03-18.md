# 워크로그 - 2026-03-18 (Day 2)

## 오늘의 목표
데이터 보강, 임베딩 모델 교체, 멀티턴 테스트, 코드 품질 개선

---

## 완료한 작업

### 1. Cywell 교육 PDF 데이터 추가
`data/raw/cywell/` 에 교육 자료 4개 PDF 추가 후 인덱스 리빌드
- 네트워킹.pdf: Service, Ingress, Route 개념
- 변수.pdf: ConfigMap, Secret 설정 관리
- 스토리지.pdf: PV, PVC, StorageClass
- 애플리케이션.pdf: Pod, Deployment, StatefulSet 등

벡터 수: 1207 → **1278** (+71)

### 2. 임베딩 모델 multilingual 교체
한국어 질의 검색 정확도 개선을 위해 모델 교체:
- Before: `all-MiniLM-L6-v2` (영어 특화)
- After: `paraphrase-multilingual-MiniLM-L12-v2` (다국어)
- 차원 384 동일 → 인덱스 구조 변경 없이 리빌드만 수행

검색 스코어 개선:
| 질의 | Before | After |
|------|--------|-------|
| "Deployment와 StatefulSet 차이" | ~0.42 | **0.73** |
| "ConfigMap과 Secret 차이" | ~0.35 | **0.67** |
| "CrashLoopBackOff 대응" | ~0.40 | **0.63** |

### 3. 멀티턴 6턴 대화 테스트
`scripts/test_multiturn.py` 작성하여 체계적 테스트 수행:
- Turn 1: 기본 질문 → 정상 응답
- Turn 2: "그걸 모니터링하는 방법" → "Pod를 모니터링하는 방법" 으로 리라이트 ✅
- Turn 3: "리소스 제한 설정" → "Pod의 리소스 제한 설정" 맥락 유지 ✅
- Turn 4: "그거 YAML 예시" → "Pod 리소스 제한 YAML 예시" 리라이트 ✅
- Turn 5: "더 알려줘" → 맥락 기반 추가 정보 제공 ✅
- Turn 6: "ConfigMap Secret 차이" → 주제 전환, 변수.pdf 검색 ✅

### 4. 캐시/리라이터 튜닝
멀티턴 테스트에서 발견한 이슈 수정:
- 캐시 threshold: 0.92 → **0.95** (다른 질문이 캐시 히트되는 오탐 방지)
- 쿼리 리라이터 짧은 질문 기준: 5자 → **8자** ("더 알려줘" 리라이트 누락 수정)

### 5. 코드 주석 보강 (자연화)
각 모듈에 설계 의도, 시행착오, TODO/FIXME 주석 추가:
- vectorstore: n_probe 변경 이유, K-Means seed 고정 이유
- pipeline: top_k*3 recall 개선, 에러 응답 캐시 방지
- llm: connection pool TODO, reasoning_content 설명
- chunker: 대용량 PDF FIXME

### 6. README 업데이트
- 임베딩 모델명 업데이트 (multilingual)
- 벡터 수 1278로 업데이트
- Cywell 교육자료 데이터 소스 추가
- 프로젝트 구조에 cywell 디렉토리 반영

---

## 발견한 이슈 & 해결

### 이슈 1: 캐시 오탐 (Turn 3)
- **증상**: "리소스 제한 설정"이 "Pod 모니터링"과 유사하다고 캐시 히트
- **원인**: multilingual 모델이 한국어 의미를 더 잘 잡아서 오히려 유사도가 높게 나옴
- **해결**: threshold 0.92 → 0.95로 상향

### 이슈 2: "더 알려줘" 리라이트 누락 (Turn 5)
- **증상**: "더 알려줘"(6자)가 리라이트 안 됨
- **원인**: 짧은 질문 기준이 5자 미만이라 6자인 "더 알려줘"가 걸리지 않음
- **해결**: 기준을 8자 미만으로 상향

### 이슈 3: PDF 텍스트 공백 문제
- **증상**: PDF에서 추출한 텍스트에 불필요한 공백 포함 ("Pod 는", "Service 가")
- **원인**: PyPDF2의 한국어 텍스트 추출 특성
- **영향**: 검색에는 큰 영향 없음 (chunker 정규화에서 어느 정도 처리됨)
- **상태**: 모니터링 중, 심각하면 PDF 전용 후처리 추가 예정

---

## 현재 상태

| 항목 | 상태 | 수치 |
|------|------|------|
| 벡터 인덱스 | 가동 중 | **1278** 벡터, 16 클러스터 |
| 문서 수 | **36개** | 스크래핑 17 + 직접작성 3 + 합성 8 + 교육자료 5 + 기타 3 |
| 임베딩 모델 | **multilingual** | paraphrase-multilingual-MiniLM-L12-v2 |
| 캐시 threshold | **0.95** | 오탐 방지 강화 |
| 멀티턴 테스트 | **6턴 통과** | 대명사 복원, 주제 전환 정상 |
| Git 커밋 | 18개 | Day1 14 + Day2 4 |

## 내일 할 일
- [ ] 합성 문서 내용 검수 (hallucination 체크)
- [ ] 추가 질의 시나리오 테스트 (엣지 케이스)
- [ ] Windows 환경 테스트
- [ ] 발표 자료 준비 시작
- [ ] 코드 리뷰 대비 학습 (각 모듈 핵심 로직)
