# control_tower_corpus_playbook_observability_20260420 / reviewer

- blocked: session-level developer rule does not allow `spawn_agent` unless the user explicitly requests delegation
- intended focus:
  - verify safe delete scope and chunk-inspector UX without widening official artifact deletion
