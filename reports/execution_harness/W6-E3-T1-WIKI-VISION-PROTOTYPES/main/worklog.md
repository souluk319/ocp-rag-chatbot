# Local Worklog

- task_id: `W6-E3-T1-WIKI-VISION-PROTOTYPES`
- lane_id: `main`
- role: `main`
- user-visible progress는 milestone 종료 전까지 기록하지 않는다.
- 중간 자동 복구, 실행 메모, 임시 판단은 이 파일에만 적는다.
- kickoff: exploratory sprint day1 을 wiki vision 3안 prototype packet 으로 고정했다.
- insertion points:
  - `PlaybookLibraryPage.tsx` 상단 operational zone
  - `WorkspacePage.tsx` chat welcome zone
  - `WorkspacePage.tsx` assistant answer / viewer rail
- prototype directions:
  - `atlas_canvas`
  - `guided_tour`
  - `encyclopedia_world`
- guided_tour shell 을 answer surface 까지 확장했다.
  - `AssistantAnswer` 에 `visionMode` 분기 추가
  - `guided_tour` 일 때 `관련 문서 / 바로 볼 절차` 대신 `이 순서로 따라가면 됩니다` route block 렌더
  - 핵심 절차 2개, 이어서 열 문서 2개만 먼저 노출
  - `npm --prefix presentation-ui run build` 통과
