# submission_readme_live_tone_20260420 / main

- goal lock:
  - align the repository-facing README with the actual PBS architecture and the assignment rubric
  - verify whether the live runtime on 8765 can be safely restarted for prompt/tone pickup
  - capture evidence instead of guessing whether live tone verification was possible
- non-goals:
  - do not redefine product doctrine or runtime contracts
  - do not restart an unknown runtime blindly
  - do not mix unrelated existing worktree changes into this packet
- validation target:
  - README reflects the direct-built RAG pipeline and evaluation mapping
  - runtime ownership is identified concretely
  - if safe restart is possible, verify guide-forward answer behavior on the running service
- evidence:
  - docker compose ownership confirmed for `127.0.0.1:8765` backend and bind-mounted `./src` runtime
  - `README.md` now maps the assignment rubric to concrete PBS files and run commands
  - `docker compose restart backend` succeeded and `/api/health` recovered on `http://127.0.0.1:8765`
  - live `/api/chat` smoke returned guide-forward RBAC answer with `response_kind=rag`, `current_topic=RBAC`
  - `.\\.venv\\Scripts\\python.exe -m pytest tests\\test_app_chat_api_multiturn.py -q` passed
  - demo simulator rerun on live runtime preserved `scenario_completion_rate=1.0`, `turn_pass_rate=1.0`, `streaming_turn_pass_rate=1.0`
