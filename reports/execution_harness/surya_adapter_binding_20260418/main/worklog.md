# surya_adapter_binding_20260418

- scope locked:
  - verify Surya health/ocr endpoint
  - bind Surya to degraded PDF fallback seam
  - keep fallback optional and graceful on adapter failure
- non-goals:
  - private corpus redesign
  - Surya bulk backfill
  - graph experiments
  - viewer UX changes
- connectivity:
  - `.env` `SURYA_OCR` loaded through `load_settings(...)`
  - `probe_surya_health(settings=...)` returned `status=ok`, `ready=true`
  - live OCR probe on a generated PNG returned `ConfigMap Secret`
- implementation:
  - PDF degraded fallback now respects `.env` and uses `surya` automatically when configured
  - image normalize path now triggers optional Surya fallback only on low-confidence Docling OCR
  - remote OCR is fail-closed unless the private envelope is non-placeholder, approved, and explicitly sets remote egress + non-raw redaction
- reviewer result:
  - Rawls blocked unconditional remote OCR for private/customer drafts
  - packet was narrowed to secure binding, not blanket activation
