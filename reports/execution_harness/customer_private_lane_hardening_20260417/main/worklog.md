# customer_private_lane_hardening_20260417 / main

- scope locked:
  - treat this as a major task spanning customer/private ingest, viewer fidelity, retrieval, and security boundary
  - do not redefine product doctrine or merge private sources into the official runtime lane
  - first produce a packetized execution sequence grounded in current code and contracts
- validation target:
  - packet sequence is tied to current code paths, not abstract ideas
  - table-fidelity and private-chatbot-corpus plans each name the concrete truth source, failure mode, and verification loop
  - companion lanes are recorded before closeout
- current judgment:
  - this scope is too large for one uninterrupted packet, so execution is split into numbered packets with packet 1 as the approval gate
  - packet 1 repairs customer-pack table truth before any broader chatbot corpus promotion
  - packet 2 hardens selected-draft retrieval behavior using the existing overlay path
  - packet 3 materializes a first-class private corpus lane derived from the same canonical customer-pack truth
  - packet 4 adds security-boundary and mixed-output smoke gates
- packet 1 closeout:
  - markdown table serialization now trims sparse placeholder columns and collapses degenerate PDF tables into compact rows
  - [TABLE header="false"] is now preserved into canonical truth and honored by the viewer so compact rows are rendered as body rows instead of fake headers
  - section normalization now treats [CODE]/[TABLE]/[FIGURE] blocks as atomic, preventing numeric table rows from splitting sections
  - actual sample draft dtb-0792b70c78f0 was regenerated and now stores the ConfigMap/Secret comparison as compact rows in the operation playbook artifact
- packet 2 kickoff:
  - goal is to harden the existing uploaded overlay lane before any first-class private corpus build
  - selected draft scope must survive BM25 overlay filtering, fusion, rerank, and final context assembly
  - copied snippets such as ConfigMap Secret should still land on the active uploaded draft even without an explicit "uploaded document" phrase
- companion lanes used:
  - explorer `Dirac` audited active-draft overlay retrieval path, fusion, rerank rescue ordering, and likely root-cause boundaries
  - explorer `Meitner` audited the best existing regression tests and helpers for exact-copy, uploaded-draft preference, and citation behavior
- packet 2 closeout:
  - customer-pack draft filtering now resolves the root draft id from viewer_path before chunk_id so derived assets under `/playbooks/customer-packs/<draft>/assets/...` survive active selection
  - retrieval pipeline uploaded preservation already honored active draft scope; that contract is now regression-locked with a bare-snippet test that forces official bm25+vector support to compete against the uploaded hit
  - rerank uploaded rescue remains the last rebalance rule so selected uploaded hits are restored after later heuristics and the trace clearly records `uploaded_customer_pack_priority`
  - answer context now seeds uploaded evidence for active selected-draft scope, not just explicit uploaded-document phrasing
  - targeted packet regressions passed: derived-asset filter, explicit uploaded query, selected-draft snippet query, and context seeding
  - broader changed-file run still shows two pre-existing etcd rerank failures unrelated to this packet; they are recorded in final_report.json and left for a separate packet
- packet 3 kickoff:
  - goal is to promote customer-pack retrieval from raw overlay-only behavior to a first-class private corpus lane derived from the same canonical customer-pack truth
  - official corpus and official Qdrant collection remain untouched; selected uploads must materialize into their own artifact lane under `artifacts/customer_packs/corpus`
  - private retrieval stays selected-draft scoped and mixed citation truth must remain `mixed_runtime_bridge` when official and private citations appear together
- companion lane findings used:
  - explorer `Galileo` confirmed the safest insertion point is `CustomerPackNormalizeService.normalize()` immediately after canonical + derived payloads are finalized and before the draft record is marked normalized
  - explorer `Parfit` confirmed the minimum regression set is the existing selected-draft retrieval tests plus one new mixed official/private citation-truth test around `_summarize_citation_truth`
- packet 3 closeout:
  - normalize now materializes a first-class private corpus artifact under `artifacts/customer_packs/corpus/<draft_id>/` with `chunks.jsonl`, `bm25_corpus.jsonl`, `vector_store.jsonl`, and `manifest.json`
  - the private corpus is derived from the same canonical customer-pack truth and derived playbook assets that feed the user library, preserving the shared-truth contract
  - retrieval now prefers selected-draft private BM25 artifacts before falling back to the old raw overlay path, so active uploaded scope no longer depends on searching every book JSON first
  - private vector retrieval is now local-artifact based and selected-draft scoped; it does not touch the official Qdrant collection and can operate without an official vector retriever
  - intake API responses and draft payloads now expose `private_corpus` manifest metadata so the runtime can verify that uploaded truth was promoted into corpus form
  - mixed official/private citation truth is now regression-locked with a focused `_summarize_citation_truth` test
- packet 4 kickoff:
  - goal is to formalize a maintenance gate, not add a new product surface
  - the gate must prove `ingest -> private corpus manifest -> customer-pack book -> viewer document -> selected chat private citation -> unselected no leak -> cleanup`
  - smoke output must keep boundary evidence and status, but should not dump raw local filesystem paths or private artifact internals beyond the required envelope summary
- packet 4 companion lane findings used:
  - explorer `James` confirmed the smallest safe route is to add a maintenance-smoke style module plus CLI command, without new HTTP endpoints
  - worker `Halley` flagged two residual risks outside this packet scope: intake API still exposes internal filesystem paths, and retrieval hard scope is still draft-id PoC scope rather than tenant/workspace/pack-version hard scope
- packet 4 closeout:
  - added `src/play_book_studio/app/private_lane_smoke.py` as the canonical private-lane maintenance gate with `build_* -> write_* -> summary.ok` contract
  - the smoke now validates the required private security envelope and boundary labels on the private corpus manifest before probing book, viewer, selected chat, unselected chat, and cleanup
  - smoke output was redacted down to boundary metadata, counts, and viewer/runtime truth labels instead of dumping raw ingest payload paths
  - added `private-lane-smoke` to the canonical CLI so maintenance can run the same gate through the standard entrypoint
  - added `tests/test_private_lane_smoke.py` with a temp-server end-to-end regression and boundary-helper coverage, plus CLI parser/runner coverage in `tests/test_cli.py`
  - reran private-corpus retrieval regressions to confirm packet 3 selected-draft bm25/vector behavior and context seeding still hold after the new gate landed
